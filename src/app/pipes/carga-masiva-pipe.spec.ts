import { CargaMasivaPipe } from './carga-masiva-pipe';

describe('CargaMasivaPipe', () => {
  it('create an instance', () => {
    const pipe = new CargaMasivaPipe();
    expect(pipe).toBeTruthy();
  });
});
